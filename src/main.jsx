import React from "react";
import ReactDOM from "react-dom/client";
import ReachStackerDashboard from "./ReachStackerDashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ReachStackerDashboard />
  </React.StrictMode>
);
